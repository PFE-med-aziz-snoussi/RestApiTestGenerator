import { Version } from "./version.model";

export class Execution {
    id?: number;
    fichierResultCollection?: string;
    version?: Version;
    createdAt?: string;
    updatedAt?: string;
  }
  